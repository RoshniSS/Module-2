# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
a = int(input())
b = int(input())

f = lambda a, b: a + b

print(f(a, b))

## Output
<img width="1436" height="313" alt="Screenshot 2026-06-01 111318" src="https://github.com/user-attachments/assets/8f6b5cec-ad8f-44fa-9c5c-c00e3b93d9b4" />

## Result
Thus, the Python program to add two numbers using a lambda function was executed successfully, and the desired output was obtained.
